<%= form_for(@motivo_movimiento) do |f| %>
  <% if @motivo_movimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motivo_movimiento.errors.count, "error") %> prohibited this motivo_movimiento from being saved:</h2>

      <ul>
      <% @motivo_movimiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
